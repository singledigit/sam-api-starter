{
  "name": "api-starter",
  "version": "0.0.1",
  "devDependencies": {
    "jest": "^24.7.1",
    "babel-jest": "22.4.3"
  },
  "scripts": {
    "test": "jest --bail --coverage --coverageDirectory=output/coverage/jest"
  }
}
