{
  "name": "api-starter",
  "version": "0.0.1",
  "devDependencies": {
    "aws-sdk": "^2.697.0",
    "babel-jest": "22.4.3",
    "jest": "^24.7.1"
  },
  "scripts": {
    "test": "export TEST_RUN=true && jest --bail --coverage --coverageDirectory=output/coverage/jest && export TEST_RUN=false"
  }
}
